# Supabase (server-only — service role bypasses RLS; never expose to the client)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Resend
RESEND_API_KEY=
ORDER_NOTIFICATION_EMAIL=ben@matr.art
# Prod: orders@matr.art (verify matr.art in Resend). First test: onboarding@resend.dev
ORDER_FROM_EMAIL=onboarding@resend.dev
