/** Shared constants used by both the client (validation) and server (enforcement). */

/** Max size for an uploaded artwork image, in bytes (5 MB). */
export const MAX_IMAGE_BYTES = 4.5 * 1024 * 1024;

/** Human-readable form of {@link MAX_IMAGE_BYTES} for UI copy. */
export const MAX_IMAGE_LABEL = "4.5MB";
